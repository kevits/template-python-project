# The .env file placed next to your compose.yaml is loaded automatically by Docker Compose
# https://docs.docker.com/compose/how-tos/environment-variables/variable-interpolation/#env-file
COMPOSE_PROJECT_NAME=template-python-project

# Following environment variables should be set in a separate environment file named .env.local
MCP_CONTEXT7_API_KEY=
